/*document.write
    alert()/confirm
    prompt()
    parseInt()
    parseFloat()
*/

/*
    const : valeur consante (non modifiable), pour nombres, chaines, booléens,....
    types de données constant, mais contenu modifiable pour tableaux, objets,... 
*/    

/*var name = prompt ("Comment t'appelles tu ?");

document.write(name);*/

/*opérateurs*/
/* 
    = , += , -= , *= , /= , %=

    ++ , --

    + , - , * , / , %

    de comparaison: <, >, <=, >=

    d'égalité: ==, ===, !=, !==
*/

/*
    N = N+1;// N += 1
    N = N-2;// N -= 2
*/

/*
    N += 1 (incrémenter)
    N -+ 1 (décrémenter)

    N++;/++N; (N++(post-incrémentation) // ++N(pré-incrémentation) )

    N--;/--N; (N--(post-décrémentation) // --N(pré-décrémentation) )

    (que pour le plus ou le moins)

    N++
        -> N = N
        -> N + N

    ++N
        -> N + 1
        -> N = N
*/

/*document.write (8 !== 8);

let result = (9 + 4) / 7 * 2 ;
document.write (result);*/

/*let name = "Yuna";*/

/*
    if(name == "Yuna")
        document.write("OK");
    else
        alert("Pas OK");
*/

/*switch(name)
{
    case "Yuna":
        document.write("OK");
        break;

    default:
        alert("Pas OK");
        break;
}*/

/*Ternaire

Validité: <condition> ? <cas_vrai> : <cas_faux>


let number = 15;
let res = (number > 10) ? true        :     false;
                        "number > 10" : "number <= 10";

document.write(res);
*/